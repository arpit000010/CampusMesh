# Example env for client — copy to .env.local for local dev
# In Vercel, set these as Environment Variables in the dashboard
VITE_API_URL=http://YOUR_ORACLE_VM_IP/api
VITE_SOCKET_URL=http://YOUR_ORACLE_VM_IP
