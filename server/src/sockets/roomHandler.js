// Room Handler — handles join_room, leave_room events
// Will be implemented in Phase 6
export const registerRoomHandlers = (io, socket) => {
  // Phase 6: room join/leave events will be registered here
};
